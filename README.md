# 企业简易报销系统

基于 PHP + SQLite 的轻量级报销管理系统，支持多主体、发票识别、附件打包下载。

## 功能特性
- 扫码识别发票/PDF金额与日期
- 自动计算器输入框
- 管理员代填功能
- 一键审核通过
- 多公司主体核算

## 部署说明
1. 将代码上传至 Web 服务器 (Apache/Nginx)。
2. 确保 `db/` 和 `uploads/` 目录具有写入权限 (777)。
3. 访问 `install.php` 或首页自动初始化数据库。
4. 默认管理员: admin / 123456
